# Nixpacks configuration for Railway
[phases.setup]
nixPkgs = ["nodejs_18", "python3", "gcc", "gnumake"]

[phases.install]
cmds = ["npm ci"]

[phases.build]
cmds = ["mkdir -p database"]

[start]
cmd = "npm start"
