# Nixpacks configuration for Railway
[phases.setup]
nixPkgs = ["nodejs_18", "python3", "gcc", "gnumake", "libtool", "autoconf", "automake"]

[phases.install]
cmds = ["npm ci", "npm rebuild better-sqlite3"]

[phases.build]
cmds = ["mkdir -p database"]

[start]
cmd = "npm start"
